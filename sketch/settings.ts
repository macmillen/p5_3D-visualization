class Settings {
    mode = MODE.Wobble;
    colorMode = COLOR_MODE.White;
    shape = SHAPE.Cube;

    spinningRects = false;
    spheres = true;
    faces = false;
    lines = true;
    strokeWeight = 5;

    autoRotate = false;

    sphereRadius = 10;
    sphereDetailX = 6;

    spinningSize = createVector(50, 30, 150);

    wobbleStrength = 75;

    lineLengthX = 200;
    lineLengthY = 100;
}